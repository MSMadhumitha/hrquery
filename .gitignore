# Environment variables & secrets
.env
.env.*
!.env.example

# Python cache & build
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
build/
dist/
*.egg-info/

# Pytest cache
.pytest_cache/
.coverage
htmlcov/

# Vector Store persistence (keep directory structure via .gitkeep)
vector_store/*.faiss
vector_store/*.json
vector_store/*.pkl

# Uploads directory contents (keep directory structure via .gitkeep)
data/uploads/*
!data/uploads/.gitkeep

# IDE & OS
.vscode/
.idea/
.DS_Store
Thumbs.db
